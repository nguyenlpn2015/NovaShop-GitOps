apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Phase 1 is intentionally the default. Activating TLS requires a reviewed
  # GitOps change from phases/http to phases/tls.
  - phases/http
