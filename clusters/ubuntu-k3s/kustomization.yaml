apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # TLS staging keeps HTTP available while validating ACME issuance.
  # Production certificates, redirects, and HSTS require a separate promotion.
  - phases/tls
